# Cleanup Pass 3 Evidence-Only Risk Report

Sorted by severity, then confidence.

| issue | severity | file_path | why_risky | confidence | exact_evidence | suggested_fix | dead_code_status |
|---|---|---|---|---|---|---|---|
| Tracked local config contains live credential material | Critical | `js/config.local.js` | Credential leakage risk and key reuse risk if file is ever pushed/shared; enables unauthorized API use with exposed keys. | High | `js/config.local.js:5` sets `window.SUPABASE_URL`; `js/config.local.js:6` contains full `SUPABASE_ANON_KEY`; `js/config.local.js:12` contains Google API key. | Remove tracked secrets, rotate exposed keys, enforce `.gitignore` + CI secret scanner for `config.local.js` patterns. | likely dead code |
| Browser runtime-config endpoint can leak future secrets by config drift | High | `netlify/functions/runtime-config.js` | Any env var added here is sent to all clients; accidental addition of privileged secret would be immediate exposure. | High | `runtime-config.js:8-22` serializes env vars into `window.*`; includes auth/config keys and feature flags in public JS response. | Convert to explicit audited allowlist with deny-pattern tests (`SECRET`, `SERVICE_ROLE`, `PRIVATE_KEY`), add CI guard. | likely dead code |
| Legacy permissive anon policy definitions can be misapplied | High | `supabase/schema.sql` | `Allow all for anon` policy template creates full read/write exposure if applied in wrong environment. | High | `supabase/schema.sql:176-182` creates `FOR ALL TO anon USING (true) WITH CHECK (true)` across key tables. | Mark file as local bootstrap-only, block production apply path, or remove permissive policy block from canonical deployment artifact. | likely dead code |
| Product/profile schema drift can break runtime DB calls | High | `js/db-supabase.js` | Feature-gated fields (`status`, pipeline columns) cause runtime failures when schema/cache mismatches occur in live branch drift. | High | `db-supabase.js:16-30` conditionally includes pipeline columns; direct `.eq('status', ...)` query at `db-supabase.js:679`; prior live error history confirms this failure mode. | Add adapter preflight for expected columns per product profile; fail fast with actionable UI and CI gate. | likely dead code |
| Invalid global assignment bug in local config flag | Medium | `js/config.local.js` | Missing `window.` prefix can create inconsistent behavior and hard-to-debug flag state. | High | `js/config.local.js:18` uses `ENABLE_AUTO_CLAIM_UNOWNED_DATA=false;` instead of `window.ENABLE_AUTO_CLAIM_UNOWNED_DATA`. | Normalize all config globals to `window.*`; add lint rule for undeclared assignments in config files. | likely dead code |
| Debug surface exposed in production bundle | Medium | `js/app.js` | Debug helpers can increase accidental operator misuse and widen attack surface when callable from console. | Medium | `js/app.js:10545` exports `window.debugNoteSave`; includes methods like `simulateSave` (`10605`) and edit verification helpers (`10908+`). | Guard debug exports behind explicit dev flag and strip in production builds. | likely dead code |
| Deprecated Supabase shim appears unreachable | Low | `js/dbSupabase.js` | Unused compatibility file increases maintenance overhead and confusion; could be accidentally referenced later. | High | Only self-references found (`rg` hit in `js/dbSupabase.js:11,15`); entrypoint chooses `db-supabase` or `db` in `index.html` dynamic loader. | Remove file or move to `tools/archive/` after one-release validation. | confirmed dead code |
| Unreferenced `js/.env` artifact | Low | `js/.env` | Unused env artifact can mislead contributors and invite insecure local practices. | High | No runtime/workflow references found in repo grep; file present under `js/` but not imported/loaded by entrypoints. | Delete file or relocate to documented local-only template outside runtime folder. | confirmed dead code |
